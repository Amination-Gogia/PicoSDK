#include <stdio.h>
#include "pico/stdlib.h"

// class Matrix;

//Matrix identity(int n);

int main()
{
    stdio_init_all();
    //Matrix A = identity(3);
    while (true) {
        printf("Hello, world!\n");
        sleep_ms(1000);
    }
}
